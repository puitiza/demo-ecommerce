server:
  port: 8761

spring:
  application:
    name: server-discovery

eureka:
  client:
    register-with-eureka: false  # The server itself is not recorded
    fetch-registry: false        # Does not consult other records
